import React from "react";

const GalleryImage = ({ image }) => {
  console.log(image);
  //   return (
  //     <div>
  //       <img src={image} alt={image} />
  //     </div>
  //   );
};

export default GalleryImage;
